package com.byazt.ec;

import androidx.annotation.NonNull;
import com.byazt.b.e;
import com.byazt.b.eb;
import com.byazt.b.gu;
import com.byazt.b.jx;
import com.byazt.b.l;
import com.byazt.b.q;
import com.byazt.b.s;
import com.byazt.b.sz;
import com.byazt.b.vv;
import com.byazt.b.zy;
import com.byazt.yk.DownloaderBuilder;

/* loaded from: classes2.dex */
public interface hp {
    hp hp(e eVar);

    hp hp(@NonNull eb ebVar);

    hp hp(@NonNull gu guVar);

    hp hp(@NonNull jx jxVar);

    hp hp(@NonNull l lVar);

    hp hp(@NonNull q qVar);

    hp hp(@NonNull s sVar);

    hp hp(sz szVar);

    hp hp(vv vvVar);

    hp hp(@NonNull zy zyVar);

    hp hp(com.byazt.f.eb ebVar);

    hp hp(com.byazt.f.jx jxVar);

    hp hp(@NonNull com.byazt.v.hp hpVar);

    hp hp(DownloaderBuilder downloaderBuilder);

    hp hp(String str);

    void hp();
}
